export const selectTeacherName = (state) => state.teacher.name;

export const selectTeacherToken = (state) => state.teacher.token;

export const selectTeacherId = (state) => state.teacher.id;

export const selectTeacherSubjects = (state) => state.teacher.subjects;

export const selectTeacherStudents = (state) => state.teacher.students;
