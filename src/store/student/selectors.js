export const selectStudentName = (state) => state.student.name;

export const selectStudentToken = (state) => state.student.token;

export const selectStudentId = (state) => state.student.id;

export const selectStudentSubjects = (state) => state.student.subjects;
